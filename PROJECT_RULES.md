# Project Rules

## Commit Message Rules
- All commit messages must be provided inside a markdown text code block.
- Commit messages must NOT contain any file paths (e.g., avoid path/to/file.tsx).
- Commit messages should ONLY be provided when explicitly requested by the user.
- Format:
  ```text
  [type]: [description]

  - [bullet point]
  ```
